my:
  xxx: 222

spring:
  cloud:
    nacos:
      server-addr: 127.0.0.1:8848
      config:
        file-extension: yaml
  config:
    import:
      - optional:nacos:${spring.application.name}.yaml # 导入文件优先级 > 本文件